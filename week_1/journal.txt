26.07.2019 
    Setting up git environment
    Commannds to remember:

    to save login and password:
    git config (--global) credential.helper store
    git pull
    
    git config --global user.email test@star-s.ru  (e-mailsetup)


    git pull - get latest changes on current branch

    branching
        git checkout [branchname] switch branches
        git checkout -b [branchname] create branch and switch to it
        git add .
        git commit -m [messagetext]

    merging process:
        git checkout master
        git merge [branchname]
        git status
        git commit -m [msgtext] (or git push directly...)
        git push origin master